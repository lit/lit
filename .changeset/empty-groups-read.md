---
'lit-html': minor
'lit': minor
---

Add a `partMap` directive for declaratively building the part attribute from an object of boolean flags, similar to `classMap`. This also introduces the `DOMTokenMapDirective` base class to share logic between space-delimited attribute directives.
