{
  "name": "lit-localize-examples-runtime",
  "description": "Example of using lit-localize in runtime mode",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rimraf lib/* && lit-localize build && tsc",
    "serve": "web-dev-server --node-resolve"
  },
  "dependencies": {
    "@lit/localize": "^0.10.0",
    "@material/mwc-circular-progress": "^0.18.0",
    "lit": "^2.0.0-rc.1"
  },
  "devDependencies": {
    "@lit/localize-tools": "^0.3.0",
    "@web/dev-server": "^0.1.10",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}
