{
  "name": "lit-localize-examples-runtime-js",
  "description": "Example of using lit-localize in runtime mode with JavaScript",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf src/generated && lit-localize build",
    "serve": "web-dev-server --node-resolve"
  },
  "dependencies": {
    "@lit/localize": "^0.11.0",
    "@material/mwc-circular-progress": "^0.25.1",
    "lit": "^2.0.0"
  },
  "devDependencies": {
    "@lit/localize-tools": "^0.4.0",
    "@web/dev-server": "^0.1.22"
  }
}
